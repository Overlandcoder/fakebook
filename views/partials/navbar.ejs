<nav class="bg-white border-b border-gray-200 sticky top-0 shadow-sm">
  <div class="max-w-6xl mx-auto px-4 h-16 flex justify-between items-center">
    <div class="flex items-center gap-2">
      <a href="/" class="text-2xl font-black text-green-600">Fakebook</a>
    </div>

    <div class="flex items-center gap-4">
      <% if (currentUser) { %>
        <a href="/" class="p-2 hover:bg-gray-100 rounded-full transition-colors text-gray-600" title="Home">
          <svg xmlns="http://w3.org" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
          </svg>
        </a>
        <a href="/users/<%= currentUser.username %>" class="flex items-center gap-2 py-1 px-3 rounded-full transition-colors hover:bg-gray-100">
          <div class="w-8 h-8 flex items-center justify-center bg-green-100 text-green-700 font-bold text-xs rounded-full">
            <%= currentUser.username[0].toUpperCase() %>
          </div>
          <span class="font-semibold text-sm text-gray-700 hidden sm:block"><%= currentUser.username %></span>
        </a>
        <form action="/logout" method="POST">
          <button type="submit" class="text-sm text-gray-500 hover:text-red-600 font-medium">Log Out</button>
        </form>
      <% } else { %>
        <a href="/login" class="px-4 py-2 text-sm font-semibold text-green-600 rounded-lg hover:bg-green-100">Log In</a>
        <a href="/signup" class="px-4 py-2 text-sm font-semibold text-white bg-green-600 rounded-lg hover:bg-green-700">Sign Up</a>
      <% } %>
    </div>
  </div>
</nav>